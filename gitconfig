[user]
	name = scheakur
	email = scheakur@gmail.com

[core]
	excludesfile = ~/.gitignore
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = 
[mergetool "sourcetree"]
	cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
[github]
	user = scheakur
[alias]
	revertlocalchange = checkout HEAD --
	svnco = svn clone --localtime
	svnup = !git stash && git svn fetch --localtime && git svn rebase --local && git stash pop
	svnci = !git svn fetch --localtime && git svn rebase --local && git svn dcommit --no-rebase
	co = checkout
	s = status -s
	st = status
	ci = commit
	cia = commit -a
	ciav = commit -av
	di = diff -w
	dic = diff -w --cached
	br = branch
	div = difftool --gui --no-prompt
	svn-ref = "!f() { ret=''; for rev in $@; do ret=${ret}' '`git log --grep "git-svn-id.*@${rev}" --pretty=%H;`; done; echo $ret; }; f"
	lg = log --color --graph --pretty=format:'%C(green)%h%Creset %C(cyan)(%cd)%C(yellow)%d%Creset %s %C(blue)<%an>%Creset' --date=local --abbrev-commit
	update-remote = remote update --prune
[color]
	ui = auto
[diff]
	tool = vimdiff
	guitool = gvimdiff
	algorithm = histogram
[filter "media"]
	required = true
	clean = git media clean %f
	smudge = git media smudge %f
[ghq]
	root = ~/src
[push]
	default = simple
[filter "hawser"]
	clean = git hawser clean %f
	smudge = git hawser smudge %f
	required = true
[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
